# The deployed image installs VeADK from a public package source. Before using
# this example on AgentKit, ensure the selected veadk-python version includes
# the piagent runtime added in this branch. For pre-release cloud testing,
# replace this line with a pushed git branch or an internal wheel URL.
veadk-python

fastapi
uvicorn
