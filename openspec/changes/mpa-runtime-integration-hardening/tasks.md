# Tasks

- [x] `T-1` Review and approve the bilingual PRD/component contracts. (`VC-1`–`VC-8`)
- [x] `T-2` Add the VeADK-only AppCenter discovery and lazy-login boundary. (`VC-1`)
- [x] `T-3` Inject Runtime URL/key in phase two and enable APMPlus without trace content. (`VC-2`, `VC-7`)
- [x] `T-4` Correct first-artifact append semantics and relay reasoning/usage. (`VC-3`, `VC-5`)
- [x] `T-5` Bound Runtime logs to 1,000 sanitized lines and download the visible snapshot. (`VC-4`)
- [x] `T-6` Implement usage snapshot/delta normalization through the existing Token UI. (`VC-5`)
- [x] `T-7` Implement allowlisted request-scoped model selection for primary and delegated execution. (`VC-6`)
- [x] `T-8` Run targeted suites, frontend full tests/build, static checks, pre-commit, and two review rounds. (`VC-8`)
- [x] `T-9` Publish a new mpa-agent image, update Runtime `r-yeuujrrcowb21078p9jh`, and execute live cases. (`VC-1`, `VC-3`–`VC-7`)
- [ ] `T-10` Commit and push scoped changes in both repositories after live gates pass. (`VC-8`)

## Alignment

All `FR-1`–`FR-9` requirements map to `VC-1`–`VC-8`. No Task lacks an acceptance case. Existing user-owned changes and untracked configuration files are excluded from scoped commits.
