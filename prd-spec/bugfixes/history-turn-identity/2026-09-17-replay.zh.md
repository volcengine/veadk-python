# 保持历史对话顺序

[English](2026-09-17-replay.md)

用户于 2026-09-17 批准。复现：问题一、回答一、问题二、回答二被渲染成问题一、回答二、问题二。每条用户消息都重建 projector，在相同 `adk-history` 前缀下重置计数器，全局更新因此覆盖前面的回答。

要求：保留历史回复及顺序，包括同轮多个作者和多个最终回复。范围：`eventsToTurns` 与回归测试。新建历史 projector 时使用当前已累计的 turn 数作为前缀，保留 projector 内部序号。不修改存储事件、后端接口、流式处理或视觉样式。无组件契约变更：恢复已有历史忠实展示语义。

计划：先复现多轮用例失败，仅修改历史 projector 前缀，验证缺少 invocation ID 和多回复场景，新增行覆盖率超过 95%，执行前端回归、构建、资源检查和 pre-commit，最后在本地重新打开真实历史会话。已直接完成设计审查：每次重建前都会插入用户 turn，故计数保证唯一；工具响应仍按 author 分类。不增加依赖或迁移数据。

验证：前端 1102 个测试、覆盖率测试 6 个通过；修改的生产代码语句覆盖率 100%。构建、102 个打包文件 / 246 个引用、pre-commit 和 diff 空白检查均通过。不涉及后端或 Runtime 镜像变更。

浏览器实测通过：重新打开问题对应的 Runtime 会话 `231046f6-789c-4e40-bda6-d687224857ea`，两个回答分别保留在各自问题之后，会话以第二个回答结束。本地 Studio 已重建并重启。
