# Preserve historical conversation order

[中文](2026-09-17-replay.zh.md)

Approved by the user on 2026-09-17. Evidence: replaying question 1, answer 1, question 2, answer 2 produces question 1, answer 2, question 2. Every user boundary resets the projector counter under the same `adk-history` prefix; global turn upsert replaces the earlier answer.

Requirement: preserve all historical replies and their order, including multiple authors and final replies per user turn. Scope: `eventsToTurns` and regression tests. Give each newly created history projector a prefix based on the accumulated turn count, retaining its per-projector sequence. Do not change stored events, backend APIs, stream behavior, or rendering style. No component contract changes: this restores existing history fidelity.

Plan: reproduce with a failing multi-turn regression, change only the history projector prefix, test missing invocation IDs and multiple replies, measure changed-line coverage above 95%, run frontend regression/build/assets and pre-commit, then reload the real historical session locally. Review performed directly: uniqueness holds because each reset follows insertion of a user turn; tool responses continue using author-based classification. No new dependencies or data migration.

Verification: 1102 frontend tests and 6 coverage tests passed; modified production statement coverage 100%. Build, 102 bundled files / 246 references, pre-commit and diff whitespace checks passed. No backend or Runtime image changes.

Live browser pass: reopened the reported Runtime session `231046f6-789c-4e40-bda6-d687224857ea`; both answers remain after their respective questions, and the conversation ends with the second answer. Local Studio rebuilt and restarted.
