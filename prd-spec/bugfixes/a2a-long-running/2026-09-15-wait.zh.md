# A2A 长耗时请求状态

[English](2026-09-15-wait.md)

变更：a2a-long-running；2026-09-15；approved。真实请求耗时 93 秒完成，但 Studio 在 30 秒提前中断，用户要求修复。范围为 Studio 桥接与现有对话进度界面，不涉及后端业务、鉴权、任务创建或自动重试。

FR-1：已鉴权的 A2A 桥接在等待上游 HTTP 前输出非终止事件 `customMetadata.a2aStatus=connecting`，只表示等待 Runtime，不代表接受任务或执行成功。Studio/探测不可用时仍保留 30 秒首事件保护；收到有效事件后等待完成、失败或取消。
FR-2：已有 submitted/working 状态通过现有进度组件展示本地化排队/执行状态，归属于当前助手轮次且不算答案，实际内容替换占位。未知状态使用普通思考占位。
FR-3：HTTP/协议错误继续上报，回复不完整仍报错，关闭流释放上游连接，不自动重试有副作用的请求。

T-1/AC-1：先补失败回归，覆盖桥接非流式/降级/流式、延迟结果、取消、重放和正常回复。T-2/AC-2：最小实现元数据投影和进度展示，不增加依赖。T-3/AC-3：执行相关 Python/前端测试、前端全量测试与构建、增量覆盖率高于 95%、Ruff/Pyright/pre-commit 与浏览器检查，同步打包资源。相关契约：specs/studio-runtime-diagnostics。

评审：用户已明确要求修复；边界与双语内容检查无阻塞。ui-ux-pro-max 在引用位置与安装位置均缺失，复用现有可访问状态组件并遵循 frontend/SPEC.md。风险：静默上游可能持续等待直至用户取消，状态不声称服务端已接受请求。不增加存储、权限或云配置。验证待执行。

2026-09-15 验证：前端全量 1099 项通过；Python Runtime 代理和 A2A 解码测试 101 项通过；4 项 Vitest 状态/元数据渲染测试通过。pytest-cov 与 Vitest V8 统计新增桥接/投影/占位可执行行 7/7 覆盖（100%），App 接线已在真实浏览器验证。确定性流测试在 connecting 后推进 120 秒仍能收到最终答案而未中断。已有取消/错误/不完整/重放回归通过。构建与 pre-commit 通过，源文件 diff 空白检查通过，生成资源中第三方模板字符串仍有空白告警。cli_frontend 的 Pyright 有 34 处改动外已有诊断。ui-ux-pro-max 不可用，复用现有可访问进度组件。localhost:8765 真实浏览器已看到等待提示随后被选定 Runtime 的完整答案替换，未请求工具调用或定时任务。已更新打包资源与本地启动路径，没有修改云端配置。窄屏与减少动效沿用未变化的进度组件，未单独进行窄视口检查。

补充检查结果：test_frontend_runtime_proxy.py 的 Pyright 有 20 处已有诊断，已从 e523cc7 基线复现。提交钩子密钥检查在 2 秒后超时，独立运行的全量 pre-commit 密钥扫描已通过。推送因 PolarisCzh 对 superops-team/veadk-python 无写权限返回 GitHub 403 而受阻。
