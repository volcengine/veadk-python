# A2A long-running request status

[中文](2026-09-15-wait.zh.md)

Change: a2a-long-running; 2026-09-15; approved. User requested the fix after a real 93-second request completed after Studio aborted at 30 seconds. Scope is the Studio bridge and existing transcript progress UI. Backend business logic, authentication, task creation and automatic retries are excluded.

FR-1: The authenticated A2A bridge emits a nonterminal `customMetadata.a2aStatus=connecting` event before waiting for upstream HTTP. It means waiting for Runtime, not accepted or successful execution. Existing 30-second first-event guard remains for unavailable Studio/probe paths; after a valid event, requests continue until completion, failure or cancellation.
FR-2: Existing submitted/working events render localized queue/execution status using the existing progress component. Status is scoped to the active assistant turn and is not an answer; real content replaces the placeholder. Unknown statuses use the normal thinking placeholder.
FR-3: HTTP/protocol failures still surface, incomplete replies remain failures, and closing the stream cleans up the upstream client. No auto-retry of side effects.

T-1/AC-1: Add failing bridge and projector regressions, including non-streaming/fallback/streaming, delayed final response, cancellation, replay and normal reply. T-2/AC-2: Implement minimal metadata projection and progress rendering without new dependencies. T-3/AC-3: Run affected Python/frontend tests, frontend suite/build, changed-line coverage above 95%, Ruff/Pyright/pre-commit and browser checks; update bundled assets. Related contract: specs/studio-runtime-diagnostics.

Review: approved user request; boundaries and translations reviewed, no blockers. Missing ui-ux-pro-max skill is not available at the referenced or installed locations; use existing accessible status component and frontend/SPEC.md. Risk: waiting for a silent upstream can persist until user cancellation; status deliberately does not claim server acceptance. No new storage, permissions or cloud configuration. Verification pending.

Verification on 2026-09-15: frontend suite 1099 passed; Python Runtime proxy and A2A decoder suite 101 passed. Four Vitest progress/metadata rendering checks passed. Changed executable bridge/projector/placeholder lines measured with pytest-cov and Vitest V8: 7/7 covered (100%); the App wiring was verified in the real browser. A deterministic stream test advances 120 seconds after connecting and receives the final answer without abort. Existing cancellation/error/incomplete/replay regressions pass. Build and pre-commit pass; source diff whitespace passes, generated vendor template strings retain whitespace warnings. Pyright on cli_frontend reports 34 existing diagnostics outside this change. UI skill ui-ux-pro-max unavailable; existing accessible progress component used. Real browser on localhost:8765 showed waiting followed by a complete reply from the selected Runtime; no tools or scheduled tasks were requested. Bundled assets and local launch path updated. No cloud configuration changes. Narrow-screen/reduced-motion behavior reuses the unchanged progress component; no separate narrow viewport run was performed.

Additional gate result: test_frontend_runtime_proxy.py Pyright reports 20 pre-existing diagnostics, reproduced from e523cc7. The commit hook secret check timed out after 2 seconds; the separate full pre-commit secret scan passed. Push is blocked by GitHub 403 for PolarisCzh on superops-team/veadk-python.
