# Studio 定时任务端到端接入

[English](2026-09-15-integration.md)

2026-09-15；用户已批准。目标为本地 Studio 与明确指定的北京 Runtime。FR-1：通过对话与页面完成定时任务创建/修改/触发/列表，两者使用相同用户归属。FR-2：由已鉴权的 Studio 主体注入 x-user-id，不信任浏览器任意身份头。MPA A2A 仅在 JWT 关闭时采用该头，已验证 TIP 身份优先；JWT 开启行为与用户隔离不变。FR-3：把旧镜像更新为现有增量 ADK 兼容接口版本，保留数据库/网络/凭证及历史任务。长流持续展示状态并收到结果，不开放全用户查询。

T-1：先写身份失败回归，再做最小修复。T-2：单测、增量覆盖率高于 95%、适用的前端测试构建及密钥扫描。T-3：发布 amd64 镜像，仅更新授权 Runtime 并保持配置，验证就绪。T-4：使用唯一名称、Web 投递的测试任务，完成对话/页面操作、交叉列表及执行成功验证，结束后禁用测试任务并保留运行证据。失败须修复重测。评审：身份解析复用现有服务链路，不增加鉴权绕过或依赖；验收以实际功能证据为准，不仅看镜像就绪。下方记录验证及限制。

## 验证记录，2026-09-15

通过：Runtime `r-yeuugf44qob21078l38i` 已 Ready，镜像 `20260915-f617c67-studio-e2e`（linux/amd64，源码 `f617c67`）。健康、就绪、list-apps、sessions 和任务列表均返回 200。保留数据库、网络、鉴权配置；独立部署关闭可选元数据启动和 AppCenter 资源发现。

通过：真实 Chrome 本地 Studio 对话完成创建、修改、触发、列表工具调用。对话任务 `5819ad31-a2f7-4077-8f22-084a46a27cd0` 达到版本 2，执行 `b990fc1d-d8f2-4c0b-9b13-d7c565e63e4b` 成功。页面按钮创建、编辑、触发任务 `51ecb600-d3b1-41c1-b810-cfec427de9e2`，版本 2，结果会话 `5047e17e-f41e-4742-be43-b515cbf438ca` 执行成功。两条任务在 Runtime 列表共同显示，2 次执行、成功率 100%，均保持禁用并使用 Web 投递。定时执行会话出现在历史列表。

实测补充修复：模型流投影忽略 `author=user` 回显，保留 `content.role=user` 的 Agent 工具响应；回归用例先失败再通过，不修改事件存储。

通过：MPA 151 个测试、Studio Python 85 个测试、前端 1100 个测试、等待/回显覆盖测试 5 个、前端构建与打包资源验证。新增 Python 身份语句及前端回显判断覆盖率 100%。Pyright 原有诊断仍在本次范围外（源码 34、测试 20），不声明整文件类型检查干净；使用仓库固定版本 pre-commit 作为 lint/安全检查。
