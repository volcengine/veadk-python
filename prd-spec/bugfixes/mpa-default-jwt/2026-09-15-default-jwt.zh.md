# VeADK MPA 部署默认 JWT 配置

用户于 2026-09-15 明确批准添加此部署默认值。MPA 部署没有 ArkClaw 身份池，但 REST 接口目前默认要求 JWT。在 build_runtime_env 默认值中添加 DISABLE_JWT_AUTH=true，保留 extra_env 最后覆盖，因此用户显式 false 优先。普通 Agent 部署、MPA 后端默认值、网关鉴权和现有 TIP 默认值不在修改范围内。

仅修改部署辅助函数、相关测试及本次双语规范。验收：未配置时为 true；显式 false 保持 false；不修改传入的覆盖字典。运行辅助函数和 CLI 调用测试，验证增量行覆盖率超过 95%，执行 Ruff、Pyright 和 pre-commit。不需要修改云资源。

设计复核：复用现有覆盖顺序，无需新配置项或分支。无未解决设计问题。用户明确要求在新分支实现，构成本次批准。

验证（2026-09-15）：实现前回归因缺少 DISABLE_JWT_AUTH 失败；环境组装和 CLI 测试共 26 项通过。pytest-cov 统计部署模块语句覆盖率为 98.65%。新增字典常量项不对应独立可执行行，其所属字典构造语句已覆盖，默认值与显式 false 的行为均有断言。新增行为全部覆盖，独立新增生产代码可执行行覆盖率不适用。Ruff、格式、密钥扫描和 diff 空白检查通过。生产文件 Pyright 通过；测试文件第 47 行存在两处未变化的诊断，已使用 HEAD 版本复现。最终检查前已 fetch 并 rebase 远端部署基线（已是最新）。没有执行云端部署或修改在线 Runtime。复核未发现新的正确性或契约问题。
