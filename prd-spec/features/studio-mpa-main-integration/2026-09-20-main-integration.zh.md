# 将 MPA Studio 改动整合到 main

用户授权：将 PolarisCzh 的全部 Studio/MPA 部署改动提入 superops-team main，包括 PR #1 之后的 trace 分页修复。基线 f497cc6，来源 c17297c。不修改原生 MPA 或线上 Runtime。

将 ba781d3 到 c17297c 的最终源码差异应用到 main，并重新构建静态资源。保留 main 的渠道管理、用户管理、组件库和渠道加密配置。导入和接口新增采用合并保留方式；先设置认证会话身份，再执行渠道专用身份逻辑。文档移除过时的全量定时任务和 Agent ID 输入说明。既有功能契约一并导入。

已自审：包含全部功能源码及测试，保留上游其他变更。下载测试使用 FileReader 兼容 jsdom Blob。补齐缺失的 esbuild 可选依赖锁条目，使干净安装可用。

验证：生产构建通过；1214 项前端测试、136 项相关 Python 测试、6 项渠道策略测试、26 项下载测试、6 项 A2A 测试通过。定时任务行覆盖率 99.71%，分支覆盖率 97.18%。资源检查覆盖 104 个文件、248 个引用。Ruff、格式、密钥扫描已通过并在最终暂存后重跑。本 PR 未重新执行云端 E2E，也未替换本地服务。保留构建生成的第三方包空白字符。既有功能覆盖率报告仅适用于各自测试范围，不宣称整个 client/Markdown 覆盖率达到 95%。
