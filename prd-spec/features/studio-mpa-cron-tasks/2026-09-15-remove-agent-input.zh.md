按用户截图的明确要求移除执行 Agent 输入框。新任务使用所选 Runtime ID 作为 agentId；编辑和复制保留原任务 agentId。执行目标仍由 Runtime 地址决定。不修改后端和鉴权。设计复核：范围最小，保留接口必填参数，中英文一致。验收：无 Agent 输入框，创建/编辑/复制参数有效，覆盖率超过 95%。用户批准：明确要求移除截图中字段。

验证：71 个专项测试通过；编辑器行覆盖率 100%，整体行覆盖率 99.71%。1096 个前端测试通过；构建、资源检查及 pre-commit 通过。用户仍有未保存的任务表单，未刷新其浏览器页面。

创建新任务不传 agentId，由 Runtime 使用配置中的 Agent 展示名称，缺失时回退运行时 Agent 名称。显式值及编辑/复制原值不变。此规则替代此前默认填写 Runtime ID 的处理。用户于 2026-09-15 明确批准；复核确认不改变鉴权和执行路由。
